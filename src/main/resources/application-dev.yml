## discovery server 端口
server:
  discovery:
    port1: 8761
##
eureka:
  instance:
    hostname: 192.168.3.197
  client:
    service-url:
      defaultZone:
        http://${eureka.instance.hostname}:${server.discovery.port1}/eureka/